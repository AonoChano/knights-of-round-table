
version: '3.9'

services:

  backend:

    build: ./src/backend

    ports:

      - "8000:8000"

    volumes:

      - ./logs:/app/logs

    environment:

      - PYTHONUNBUFFERED=1

  frontend:

    build: ./src/frontend

    ports:

      - "3000:3000"

    depends_on:

      - backend

